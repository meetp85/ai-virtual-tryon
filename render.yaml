services:
  - type: web
    name: ai-virtual-tryon
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ai_vto_project.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13